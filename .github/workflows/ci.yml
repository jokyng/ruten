name: CI

on:
  push:
    branches: [main]
    tags:
      - 'v*.*.*'  # release se activa en tags vX.Y.Z
  pull_request:
    branches: [main]
  workflow_dispatch:  # ejecutar manualmente

env:
  CARGO_TERM_COLOR: always

jobs:
  build-and-test:
    name: (${{ matrix.os }} / ${{ matrix.rust }}) BUILD-TEST
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        rust: [stable, beta]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: ${{ matrix.rust }}
          override: true

      - name: Cache Cargo
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-${{ matrix.rust }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.rust }}-cargo-

      - name: Build release
        run: cargo build --release --verbose

      - name: Run tests
        run: cargo test --verbose --all

      - name: Run example scripts (Ubuntu only)
        if: runner.os == 'ubuntu-latest'
        run: |
          echo "[+] Running example scripts"
          cargo run -- examples/blockchain.rt   || true
          cargo run -- examples/fibonacci.rtn   || true
          cargo run -- examples/math_demo.rtn   || true
          cargo run -- examples/crypto_demo.rtn || true

      - name: Package binaries
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          echo "[+] Packaging binaries"
          mkdir -p dist
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            copy target\release\ruten.exe dist\ruten-windows.exe
            powershell Compress-Archive -Path dist\ruten-windows.exe -DestinationPath dist\ruten-windows.zip
          else
            cp target/release/ruten dist/ruten-${{ runner.os }}
            tar -czf dist/ruten-${{ runner.os }}.tar.gz -C dist ruten-${{ runner.os }}
          fi

      - name: Upload artifacts
        if: startsWith(github.ref, 'refs/tags/')
        uses: actions/upload-artifact@v4
        with:
          name: ruten-${{ matrix.os }}-${{ matrix.rust }}
          path: dist/*

  lint:
    name: Lint and format
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          override: true
          components: rustfmt, clippy

      - name: Check formatting
        run: cargo fmt --all || true  # no diff

      - name: Run clippy
        run: cargo clippy --all-targets --all-features || true  # no warns
